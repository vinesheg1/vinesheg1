name: Update Dual Credly Badges

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Step 1: Fetch Personal Badges
      - name: Fetch Personal Badges
        uses: jd-35656/readme-credly-badges@v1
        with:
          credly_username: 'vinesh-eg'
          target_section: 'credly-personal' # <-- Tells the script to look for "credly-personal" markers
          github_token: ${{ secrets.GITHUB_TOKEN }}

      # Step 2: Fetch Organization Badges
      - name: Fetch Organization Badges
        uses: jd-35656/readme-credly-badges@v1
        with:
          credly_username: 'vinesh-eg.09e7ae76'
          target_section: 'credly-org' # <-- Tells the script to look for "credly-org" markers
          github_token: ${{ secrets.GITHUB_TOKEN }}
